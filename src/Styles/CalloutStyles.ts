import {
  getTheme,
  DefaultPalette,
  mergeStyles,
  mergeStyleSets,
  FontWeights,
  ICheckboxStyles,
  IDropdownStyles,
  IStackStyles,
} from "@fluentui/react";

import {
  tabStyle,  
  yearMonthStyle
} from ".";

const palette = getTheme().palette;
